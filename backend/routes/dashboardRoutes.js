/**
 * @file Phase 1 dashboard-route placeholder surface.
 * @throws {never} Module initialization does not throw.
 */
import { Router } from "express";

const router = Router();

export default router;
