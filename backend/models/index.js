/**
 * @file Phase 1 model export surface.
 * @throws {never} Module initialization does not throw.
 *
 * Model implementations are intentionally placeholders in this phase.
 */

import Department from "./Department.js";
import Organization from "./Organization.js";
import User from "./User.js";

/**
 * Current model-layer implementation phase marker.
 * @type {"PHASE_1_PLACEHOLDER"}
 */
export const MODEL_LAYER_PHASE = "PHASE_1_PLACEHOLDER";

export { Organization, Department, User };
