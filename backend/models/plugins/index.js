/**
 * @file Phase 1 model-plugin export surface.
 * @throws {never} Module initialization does not throw.
 *
 * Model plugin implementations are intentionally placeholders in this phase.
 */

export { default as softDeletePlugin } from "./softDelete.js";
