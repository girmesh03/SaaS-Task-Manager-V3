/**
 * @file Phase 1 placeholder for the soft-delete Mongoose plugin.
 * @throws {never} Module initialization does not throw.
 *
 * The actual plugin logic is deferred to Phase 2 with model implementations.
 */

/**
 * Placeholder metadata for the deferred soft-delete plugin.
 * @type {Readonly<{ implemented: false, plannedPhase: "PHASE_2" }>}
 */
const softDeletePlugin = Object.freeze({
  implemented: false,
  plannedPhase: "PHASE_2",
});

export default softDeletePlugin;
