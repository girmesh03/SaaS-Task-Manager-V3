/**
 * @file Authorization helper placeholder.
 */

/**
 * Placeholder permission resolver used during Phase 1.
 *
 * @returns {false} Always returns false in Phase 1.
 * @throws {never} This helper does not throw.
 */
export const hasPermission = () => false;

export default hasPermission;
