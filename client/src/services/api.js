/**
 * @file RTK-query API placeholder surface for Phase 1.
 * @throws {never} Module initialization does not throw.
 */

/**
 * Phase 1 API service placeholder.
 * @type {{ baseUrl: string; status: "not-configured" }}
 */
export const apiPlaceholder = {
  baseUrl: "/api",
  status: "not-configured",
};

export default apiPlaceholder;
