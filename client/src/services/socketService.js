/**
 * @file Socket service placeholder for Phase 1.
 * @throws {never} Module initialization does not throw.
 */

/**
 * Phase 1 socket service placeholder API.
 * @type {{
 *   connect: () => null;
 *   disconnect: () => null;
 * }}
 */
export const socketService = {
  connect: () => null,
  disconnect: () => null,
};

export default socketService;
